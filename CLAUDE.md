# CLAUDE.md

**Canonical instructions live in [AGENTS.md](AGENTS.md).** This file is a forwarder so that
tools looking for `CLAUDE.md` find the same content every other agent uses.

Read [AGENTS.md](AGENTS.md) for:

- Architecture overview and package hierarchy
- The **How-To Index** — task runbooks (bump Go, cut a release, add a command, …)
- Key patterns and rules (gRPC-only agent comms, never mock, orchestration is critical)
- Build & test commands

Do not add project instructions here — put them in `AGENTS.md` (or a `docs/` reference it
links to) so there is a single source of truth.
